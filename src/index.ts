// Public entry point for the og-image-studio rendering engine.
// Real exports land alongside the implementation. For now this keeps the
// module graph valid so the toolchain (tsc + vitest) has something to load.

export const ENGINE_NAME = 'og-image-studio';
